<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layoutDirection="rtl"
        android:orientation="vertical"
        android:padding="16dp"
        android:textDirection="rtl">

        <ImageView
            android:id="@+id/imgMascot"
            android:layout_width="140dp"
            android:layout_height="120dp"
            android:contentDescription="صورة تعليمية"
            android:scaleType="centerInside" />

        <TextView
            android:id="@+id/tvQuestion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="كم يساوي؟"
            android:textColor="@color/kid_text"
            android:textSize="34sp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/etAnswer"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="14dp"
            android:gravity="center"
            android:hint="اكتب الإجابة هنا"
            android:inputType="numberSigned"
            android:textSize="22sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSpeak"
                android:layout_width="0dp"
                android:layout_height="58dp"
                android:layout_margin="4dp"
                android:layout_weight="1"
                android:backgroundTint="@color/kid_primary"
                android:text="اسمع السؤال"
                android:textColor="@color/white"
                app:icon="@drawable/ic_sound"
                app:iconGravity="textStart"
                app:iconPadding="8dp"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCamera"
                android:layout_width="0dp"
                android:layout_height="58dp"
                android:layout_margin="4dp"
                android:layout_weight="1"
                android:backgroundTint="@color/kid_secondary"
                android:text="احفظ لقطة"
                android:textColor="@color/white"
                app:icon="@drawable/ic_camera"
                app:iconGravity="textStart"
                app:iconPadding="8dp"
                app:iconTint="@color/white" />
        </LinearLayout>

        <Button
            android:id="@+id/btnCheck"
            android:layout_width="match_parent"
            android:layout_height="62dp"
            android:layout_marginTop="8dp"
            android:drawableStart="@drawable/ic_check"
            android:drawablePadding="8dp"
            android:text="تحقق"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/tvFeedback"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="أنت بطل الحساب!"
            android:textColor="@color/kid_text"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/tvCloudStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@color/white"
            android:gravity="center"
            android:padding="12dp"
            android:text="حالة السحابة ستظهر هنا"
            android:textColor="@color/kid_text"
            android:textSize="14sp" />

    </LinearLayout>
</ScrollView>
